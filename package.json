{
  "name": "gitvis",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build public/app.ts --outdir dist",
    "start": "bun run build && bun run index.ts",
    "dev": "bun run build && bun --hot index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
